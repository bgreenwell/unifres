% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{unifend}
\alias{unifend}
\title{Compute uniform endpoints for functional residuals}
\usage{
unifend(object, y = NULL, fill = FALSE, ...)
}
\arguments{
\item{object}{A fitted model object. Currently supported model types include:
\itemize{
\item \link[stats:glm]{glm} - generalized linear models from the core \strong{stats}
package.
\item \link[mgcv:gam]{gam} - generalized additive models from the
\href{https://cran.r-project.org/package=mgcv}{mgcv} package.
\item \link[pscl:zeroinfl]{zeroinfl} - zero-inflated Poisson (ZIP) models from the
\href{https://cran.r-project.org/package=pscl}{pscl} package.
\item \link[VGAM:vglm]{vglm}/\link[VGAM:vgam]{vgam} - vector generalized linear/additive
models (VGLMs/VGAMs) from the \href{https://cran.r-project.org/package=VGAM}{VGAM}
package.
}}

\item{y}{Optional response vector. If \code{NULL} (the default), the response is
extracted from the fitted model object.}

\item{fill}{Logical indicating whether to expand discrete endpoints to cover
the full \eqn{[0, 1]} range. Default is \code{FALSE}.}

\item{...}{Additional optional arguments passed to methods.}
}
\value{
A matrix with two columns: \code{lwr} (lower endpoint) and \code{upr} (upper
endpoint) for each observation. Each row defines the bounds of a functional
residual's CDF.
}
\description{
Computes the lower and upper endpoints of the functional residual intervals
described in Liu et al. (2025). These endpoints define the cumulative
distribution function (CDF) for each functional residual, which follows a
Uniform(0,1) distribution under correct model specification.
}
\examples{
# Fit a Poisson regression model
set.seed(42)
n <- 100
x <- rnorm(n)
y <- rpois(n, exp(0.5 + 0.3*x))
fit <- glm(y ~ x, family = poisson)

# Compute uniform endpoints
endpoints <- unifend(fit)
head(endpoints)

# The endpoints define the functional residual CDFs
# Under correct specification, each F_i should be uniform on (lwr_i, upr_i)

}
\references{
Liu, D., Lin, Z., & Zhang, H. (2025). A unified framework for residual
diagnostics in generalized linear models and beyond. \emph{Journal of the American
Statistical Association}, 1–29.
\doi{10.1080/01621459.2025.2504037}
}
\seealso{
\code{\link[=fresiduals]{fresiduals()}}, \code{\link[=fredplot]{fredplot()}}, \code{\link[=ffplot]{ffplot()}}
}
